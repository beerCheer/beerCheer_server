const { getAllBeearsByPage, getTwelveBeersById } = require("../punk");

const getTop12Handler = async (req, res, next) => {
  // rate 테이블에서 top 12 데이터 가져오기
  //
};

module.exports = {
  getTop12Handler,
};
